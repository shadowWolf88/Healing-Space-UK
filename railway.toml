[build]
builder = "nixpacks"
# Force rebuild: v2026.01.23.2

[deploy]
startCommand = "gunicorn api:app"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[nixpacks]
# Nixpacks will auto-detect Python and install dependencies

[[deploy.healthcheck]]
httpPath = "/api/health"
initialDelaySeconds = 30
periodSeconds = 10
